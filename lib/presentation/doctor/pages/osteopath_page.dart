import 'package:flutter/material.dart';

class OsteopathPage extends StatelessWidget {
  const OsteopathPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
