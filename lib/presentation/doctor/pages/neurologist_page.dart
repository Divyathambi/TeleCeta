import 'package:flutter/material.dart';

class NeurologistPage extends StatelessWidget {
  const NeurologistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
