import 'package:flutter/material.dart';

class GeriatricsPage extends StatelessWidget {
  const GeriatricsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
