import 'package:flutter/material.dart';

class GeneralPhysiciansPage extends StatelessWidget {
  const GeneralPhysiciansPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
