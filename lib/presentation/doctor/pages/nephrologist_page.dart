import 'package:flutter/material.dart';

class NephrologistPage extends StatelessWidget {
  const NephrologistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
