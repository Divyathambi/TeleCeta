import 'package:flutter/material.dart';

class PediatricPage extends StatelessWidget {
  const PediatricPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
