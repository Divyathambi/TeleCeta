import 'package:flutter/material.dart';

class OncologistPage extends StatelessWidget {
  const OncologistPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
