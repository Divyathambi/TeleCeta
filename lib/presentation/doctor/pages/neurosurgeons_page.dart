import 'package:flutter/material.dart';

class NeuroSurgeonsPage extends StatelessWidget {
  const NeuroSurgeonsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
